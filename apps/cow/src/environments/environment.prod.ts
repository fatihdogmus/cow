export const environment = {
  production: true,
  database: {
    host: "localhost",
    port: 5432,
    username: "postgres",
    password: "q",
    database: "cow",
    logger: "advanced-console",
    logging: "all"
  }
};
